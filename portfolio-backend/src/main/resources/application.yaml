server:
  port: ${PORT:8080}

spring:
  application:
    name: portfolio-backend
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          model: gemini-3.1-flash-lite
